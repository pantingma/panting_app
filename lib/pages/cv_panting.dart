import 'package:flutter/material.dart';

class CVPanting extends StatelessWidget {

  final Widget title;

  const CVPanting({required this.title, super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }

}